Войди в проект после паузы. Прочитай в порядке:

1. `ROADMAP.md` — план, принципы, инвентарь экранов, этапы.
2. `HANDOFF.md` — верхняя запись (где остановились в прошлый раз).
3. Последние 1-2 файла в `decisions/`, если они изменились с прошлой сессии (по mtime).
4. `git status` и `git log --oneline -10` — актуальное состояние репозитория.

`presentation.md` (372 строки, продуктовый нарратив Telos) — справочник, не часть обязательного входа. Открывай селективно по диапазонам строк, когда того требует задача дня. Memory pivot-файл уже загружен в системный промпт через `MEMORY.md` — отдельным шагом не нужен.

После этого выдай мне короткий доклад в таком виде:

- **Где мы:** 3-5 строк — что за проект, текущий этап по ROADMAP, что было сделано в последней сессии (из верхней записи HANDOFF).
- **Следующий шаг:** один конкретный шаг из HANDOFF («Следующий шаг»). Если он устарел или ты видишь, что приоритет изменился — предложи скорректированный, с обоснованием.
- **Состояние репозитория:** ветка, есть ли uncommitted-изменения, последний коммит.
- **Что я должен подтвердить** перед тем, как ты начнёшь работать (например: «согласовать брендинг», «подтвердить mock-data scope», или просто «начинать со следующего шага?»).

Не делай ничего сверх этого. Не приступай к работе. Жди моего подтверждения.

Если в HANDOFF верхняя запись пустая или новый проект без прошлой сессии — скажи об этом и предложи начать с первого этапа M0 в ROADMAP.
