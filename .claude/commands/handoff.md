Заверши сессию: обнови `HANDOFF.md`, при необходимости добавь решение в `decisions/`, обнови ROADMAP.md по статусам этапов, по необходимости — auto-memory.

Шаги:

1. Прочитай текущий `HANDOFF.md`, чтобы знать, от чего обновляешься.
2. Прочитай `ROADMAP.md` и обнови stable canon, который изменился за сессию:
   - Чекбоксы `[x]` на пунктах этапов, закрытых в этой сессии.
   - «Открытые вопросы» — добавь новые, убери закрытые.
   - «Принятые решения» — если в этой сессии добавлен ADR (см. шаг 4), добавь строку-ссылку.
   - «Что НЕ делать» — добавь правило, если в сессии оно было сформулировано (через корректировку или фидбэк).
   - «Резюме одной фразой» — обнови, если закрыт milestone или сместилась позиция.
3. Обнови `HANDOFF.md` (узкий журнал — без open questions, ADR-реестра, что не делать, резюме; всё это живёт в ROADMAP):
   - **Где остановились** — 2-5 строк, главный итог именно этой сессии. Заменяй полностью.
   - **Сделано в последних сессиях** — добавь 1-3 пункта про эту сессию (старые буллеты не удаляй, но можешь сжимать самые старые если их стало > 15).
   - **Следующий шаг** — один, конкретный, чтобы следующая сессия начинала с него без раздумий («сделать X в файле Y», не «продолжать работу»).
   - **Последнее обновление** — сегодняшняя дата.
4. Если в этой сессии было принято существенное архитектурное решение (выбор между альтернативами, фиксация подхода, отказ от направления) — создай файл `decisions/YYYY-MM-DD-краткое-название.md` со структурой:
   - **Контекст** — что заставило принимать решение
   - **Решение** — что выбрали
   - **Альтернативы** — что отвергли и почему
   - **Последствия** — что это меняет в дальнейшей работе
   Не плодить ADR для тривиальных решений (выбор имени переменной, цвет компонента). ADR — для решений, к которым может вернуться следующая сессия.
5. Если в сессии всплыло что-то, что стоит помнить **между проектами** (моя роль, фидбэк, общие предпочтения) — сохрани в auto-memory по правилам системного промпта в global CLAUDE.md.
6. Покажи мне diff/итог изменений и спроси, всё ли верно, перед закрытием сессии.

Если сессия не вносила существенных изменений (только обсуждение, без кода и решений) — handoff не обязателен, скажи об этом.
